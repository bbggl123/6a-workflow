# 第七章：技术执行通用规范（工程缺口补齐）

> 本文件是 6A 权威协议的分章加载单元。工程细则唯一真相源在 `project-skeleton/best-practices/`；本文件只保留高层要点与索引，以及本节专属的 7.1–7.7 高层规范。7.8（分支保护盾）与 7.9（记忆系统）因篇幅较大，单独拆为 `references/6A-git-shield.md` 与 `references/6A-memory.md`。

## 7.1 防幻觉规范

执行「第四章通用守门员机制」全条（前置筛查 / 可信度模板 / 诱导拦截 / 分段自检 / 全程留痕 / 分级交还），详见 `references/6A-gatekeeper.md`。

## 7.2 安全操作环境 + 可观测性

- API 密钥、数据库密码等一律走 `.env`，禁止硬编码。
- 用户输入做校验与防注入；接口做权限校验；不向前端暴露内部错误。
- **工具范围约定**：明确各角色可用/禁用工具集与访问方式。
- **可观测执行**：各阶段产出、拦截、可信度标注须可被外部审视；留痕日志即可观测性载体。
- **敏感信息处理**：日志中脱敏，禁止记录密钥与个人隐私明文。

## 7.3 PR 治理与 CI 门禁

- 阶段5 的所有代码变更须以 **分支 + PR** 形式提交，禁止直接写入主分支。
- 每个 PR 必须触发 **门禁脚本（CI）**，至少包含：单元测试通过、lint/风格检查、文档与代码一致性校验、可信度标注完整性扫描（可调用 `scripts/credibility-lint.sh`）。
- 门禁不过 → PR 不可合入，回退到对应原子任务重做。
- 合入后由评估 Agent 做最终 Review 与文档沉淀。

## 7.4 测试策略

- **测试优先**：先写用例后写实现。
- **边界覆盖**：正常/边界/异常全量覆盖。
- **回归验证**：改动后验证关联旧功能不受影响。
- **反空断言**（新增）：扫描测试中无实际校验效果的断言（如 `assert True`, `assert result is not None` 无后续使用），计入反指标检查（可调用 `scripts/anti-metric.sh`）。

## 7.5 文档同步

- 代码改动同步更新对应文档；接口变更更新接口文档；新增依赖在文档说明用途与版本。

## 7.6 知识红线库（ISA Knowledge · 增强为跨任务记忆层）

维护一份跨任务的项目级 **knowledge 库**（`knowledge/` 目录），记录：
- 硬性规则（各角色红线）
- 历史上被纠正过的问题与对应修正
- 可信度标注失真案例
- **反模式库**（新增）：记录本次任务中发现的 AI 典型幻觉模式、刷分手法、规避行为
- **成功模式库**（新增）：记录经过验证的有效 prompt 模板、拆分粒度经验、验收技巧

每个新任务启动时先读取该库，避免重复踩坑；任务结束后将新发现沉淀回库。

> 这不仅是 ISA 知识库，更是 **Shared State**——所有角色的共享记忆。参考 Unwind AI Operating Graph 的设计：shared files are state。

## 7.7 外部锚点体系（Grounding Anchors）

> **没有锚点的图会退化为互确认的闭环系统。** 6A 必须定义以下不可被 AI 篡改的外部锚点：

| 锚点类型 | 具体形式 | 谁控制 | 不可篡改性来源 |
|---|---|---|---|
| **人类判断锚点** | 阶段4 人工审批签字 | 用户本人 | 物理世界的人类意志 |
| **物理验证锚点** | `exit 0` 测试 / 编译通过 / 实际运行 | 计算机硬件 | 物理定律 |
| **Git 引用完整性锚点（v3 新增）** | `git cat-file -t <hash>` / `git diff <hash> --stat` 客观校验 HEAD 与提交对象 | Git 对象库 | 内容寻址哈希不可伪造 |
| **冻结规则锚点** | 项目既有的 coding standard / 安全合规基线 / 不变式 | 团队共识（写入 knowledge 库） | 社会契约 |
| **数据现实锚点** | 真实数据库查询结果 / 真实 API 响应 / 生产环境日志 | 外部系统 | 真实世界的状态 |
| **同行评审锚点** | PR Code Review / 同事走查 | 其他人类 | 独立视角 |

> **锚点使用规则**：
> - 每个阶段至少触碰 **1 个以上** 不同类型的锚点
> - 阶段6 最终验收必须同时满足 **≥3 种** 不同类型的锚点
> - 当所有验证都来自同一锚点源时（例如只有 AI 自测通过），触发 **「互确认警报」**，强制引入外部锚点

## 7.10–7.15 工程规范（已下沉到 best-practices/）

> v4.1 起，工具使用 / 文件操作 / 版权引用 / 搜索策略 / 错误恢复 / 可视化路由的**完整细则**统一下沉到 `project-skeleton/best-practices/`，本节只保留高层要点与索引，消除"6A.md 与 best-practices 双源"的维护隐患。改一处即可，不再两处并存。

| 规范领域 | 高层要点 | 详细手册 |
|---|---|---|
| **工具使用纪律** | 先读后写、精确替换（old_str 唯一匹配）、参数顺序强制、编辑后立即验证、禁止破坏性操作（`reset --hard`/`push -f`）、命令失败 2 次停 | `best-practices/tool-usage.md` |
| **文件操作** | 只读输入区 / 工作区 / 交付输出区三层分离、新建前检查存在性、绝对路径、UTF-8/LF、大文件分段、临时文件清理 | `best-practices/file-operations.md` |
| **版权与引用** | 释义优先、单次引用 ≤15 英文词 / ≤10 汉字、每来源最多 1 引、禁全文复制 / 结构镜像、`[$TRAE_REF](url)` 行内引用 | `best-practices/search-citation.md` |
| **搜索策略** | 当前状态类问题必搜（即使"记得"也要验证）、query 简短精确带版本/年份、错误信息用原文、URL 必 fetch | `best-practices/search-citation.md` |
| **错误恢复** | 承认错误立即修正不找借口、不卑不亢、不确定就说"我查一下"不硬猜、不用 TODO 占位掩盖未实现 | `best-practices/gotchas.md` |
| **可视化路由** | 架构/流程/数据对比优先图表（Mermaid），简单问答/bugfix 纯文本；图说清一件事，先图后文 | —（本节要点即全部） |

> 以上规范是 6A 工程纪律的**唯一真相源**所在位置；6A.md 不再重复细则，避免内容漂移。
